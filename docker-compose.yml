version: '3.8'
services:
  app:
    build: ./app
    container_name: app-container
    command: python main.py
    environment:
      DB_HOST: db-container
      DB_USER: ERP
      DB_NAME: ERP
      DB_PASS: Oij1ZRi8#E#kDS^5
      FLASK_DEBUG: True
      FLASK_SECRET_KEY: a13b22ab08f2810e5a0a6620694f80315cad4b60771ad851623293357f519558
      FLASK_SESSION_COOKIE_SECURE: False
    volumes:
      - ./app:/home/python/app
    ports:
      - "8000:5000"
    depends_on:
      - db
      
  db:
    image: postgres:alpine
    container_name: db-container
    restart: always
    environment:
      POSTGRES_USER: ERP
      POSTGRES_DB: ERP
      POSTGRES_PASSWORD: Oij1ZRi8#E#kDS^5
    # volumes:
    #   - ./postgres/data:/var/lib/postgresql/data